.venv/
__pycache__/
*.py[cod]
.env
.DS_Store
.deepeval*
*.egg-info/
.pytest_cache/
service-account*.json
application_default_credentials.json

# deploy env dump contains the private key
DEPLOY_ENV.local.txt

# internal process doc, not part of the public repo
CLAUDE.md

# regenerable introspection cache
schema_cache.json

# Layer 4 session/cache store -- local runtime state, not source
data/session_store.db
data/session_store.db-wal
data/session_store.db-shm

# personal study notes, not part of the public repo
LEARNING.md
