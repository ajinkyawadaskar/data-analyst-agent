# Runtime only. Dev/eval extras live in requirements-dev.txt so the
# deployed image does not build deepeval (heavy) or streamlit.
google-cloud-bigquery>=3.25
sqlglot>=25.0
langgraph>=0.2
langchain-google-genai==4.3.5
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
